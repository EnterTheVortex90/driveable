document.addEventListener('DOMContentLoaded',()=>{const card=document.querySelector('#quizCard');if(!card)return;let last='';function shuffle(){const heading=card.querySelector('h2')?.textContent||'',answers=[...card.querySelectorAll('.answer')];if(!answers.length||answers.some(b=>b.disabled)||heading===last)return;answers.sort(()=>Math.random()-.5).forEach((b,i)=>{const label=b.querySelector('b');if(label)label.textContent='ABCD'[i];b.parentElement.appendChild(b)});last=heading}new MutationObserver(()=>{if(!card.querySelector('.explain'))shuffle()}).observe(card,{childList:true,subtree:true});});
